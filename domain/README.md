# domain — Domain Objects

`domain` contains "shared objects" that are being used by various modules such as DTOs, Common Models, or Utilities library.

`domain` should not import any other modules.

## xerror

`xerror` contains custom error domain.